# libs 侧 LLM 接口需求

## 1. 接口概述

libs 提供一个统一的 LLM 推理接口 `LLMRequest`，支持：
- 多轮对话消息
- 多组 RAG 上下文（每组独立配置）
- 流式/非流式输出
- 任务调度控制

---

## 2. 请求结构 LLMRequest

```java
public static class LLMRequest {
    // ===== 生成参数 =====
    public Integer max_tokens;      // 最大生成 token 数，0 表示不限制
    public Float temperature;       // 采样温度，范围 0.0 ~ 2.0
    public Boolean stream;          // 是否流式输出
    public Boolean thinking;        // 是否启用思考模式 (模型特定指令)

    // ===== 任务调度 =====
    public String lane;             // 推理通道: CHAT / TASK
    public Integer task_priority;   // 任务优先级: -1000 ~ 1000
    public Boolean task_preemptible; // 任务是否可抢占

    // ===== 核心数据 =====
    public List<Chunk> chunks;      // 分块数据，数量不固定
}
```

---

## 3. Chunk 分块结构

```java
public static class Chunk {
    // ===== 基础字段 =====
    public String type;  // chunk 类型: "message" / "rag"

    // ===== type="message" 时使用 =====
    // 对话消息数组，支持多轮对话
    public List<MessageItem> content;

    // ===== type="rag" 时使用 =====
    // RAG 文本数组
    public List<String> content;

    // ===== type="rag" 时的独立配置 =====
    public String uid;                          // 该组 RAG 的唯一标识
    public Boolean include_rag_hits;            // 是否返回 RAG 检索结果
    public Integer memory_rag_token_budget;     // 记忆 RAG token 预算
}

// 仅 type="message" 时使用
public static class MessageItem {
    public String role;     // 角色: "system" / "user" / "assistant"
    public String content;  // 消息内容
}
```

### 3.1 type 字段说明

| 值 | 说明 |
|-----|------|
| `"message"` | 对话消息块 |
| `"rag"` | RAG 上下文块 |

### 3.2 MessageItem.role 字段说明

| 值 | 说明 |
|-----|------|
| `"system"` | 系统消息 |
| `"user"` | 用户消息 |
| `"assistant"` | 助手消息 |

---

## 4. 处理逻辑

### 4.1 消息构建

1. 遍历所有 `type="message"` 的 Chunk
2. 按顺序合并其内部的 `MessageItem` 数组
3. 构建完整的对话上下文

### 4.2 RAG 处理

1. 遍历所有 `type="rag"` 的 Chunk
2. 每个 Chunk 独立处理：
   - 使用该 Chunk 的 `content` 数组进行向量检索
   - 根据 `include_rag_hits` 决定是否返回检索结果
   - 根据 `memory_rag_token_budget` 限制记忆 RAG 长度
   - 检索结果注入到用户消息之前

### 4.3 优先级规则

- 同一 `type` 内的 Chunk 按数组顺序处理
- 建议排列顺序：`message` → `rag`

---

## 5. 示例请求

### 5.1 完整示例

```json
{
  "max_tokens": 1024,
  "temperature": 0.7,
  "stream": true,
  "thinking": false,
  "lane": "CHAT",
  "task_priority": 0,
  "task_preemptible": false,

  "chunks": [
    {
      "type": "message",
      "content": [
        {"role": "system", "content": "你是一个友好的 Minecraft 助手"},
        {"role": "assistant", "content": "你好！有什么可以帮助你的吗？"},
        {"role": "user", "content": "我的钻石镐在哪里？"}
      ]
    },
    {
      "type": "rag",
      "content": [
        "玩家当前手持钻石镐",
        "身处主世界坐标 (100, 64, 200)",
        "附近有箱子"
      ],
      "uid": "rag_dynamic_001",
      "include_rag_hits": true,
      "memory_rag_token_budget": 2000
    },
    {
      "type": "rag",
      "content": [
        "钻石镐可以挖掘: 钻石矿石, 铁矿石, 金矿石, 红石矿石, 青金石矿石, 绿宝石矿石",
        "挖掘速度: 32 (金镐: 25, 铁镐: 30, 石镐: 15, 木镐: 10)"
      ],
      "uid": "rag_static_001",
      "include_rag_hits": false,
      "memory_rag_token_budget": 0
    },
    {
      "type": "rag",
      "content": [
        "玩家之前问过关于红石电路的问题",
        "玩家之前合成过活塞"
      ],
      "uid": "rag_memory_001",
      "include_rag_hits": true,
      "memory_rag_token_budget": 500
    }
  ]
}
```

### 5.2 最小示例（无 RAG）

```json
{
  "max_tokens": 512,
  "temperature": 0.8,
  "stream": true,
  "thinking": false,
  "lane": "CHAT",
  "task_priority": 0,
  "task_preemptible": false,

  "chunks": [
    {
      "type": "message",
      "content": [
        {"role": "system", "content": "你是一个友好的助手"},
        {"role": "user", "content": "你好"}
      ]
    }
  ]
}
```

---

## 6. 返回结构

### 6.1 流式响应

每个 chunk 返回 `StreamChunk`，包含 token 内容。

如果 `include_rag_hits=true` 的 Chunk 有检索结果，会在流开始时返回 `RagHitsChunk`：

```java
public static class RagHitsChunk {
    public String id;
    public String object = "chat.completion.rag_hits";
    public long created;
    public String model;
    public List<RagHit> rag_hits;
}

public static class RagHit {
    public String uid;          // 记忆 UID
    public double score;        // 相似度分数
    public String content;      // 原文内容
}
```

### 6.2 非流式响应

返回完整文本 + 可选的 RAG Hits。

---

## 7. 字段默认值

| 字段 | 默认值 |
|------|--------|
| `max_tokens` | 0 (不限制) |
| `temperature` | 0.7 |
| `stream` | false |
| `thinking` | false |
| `lane` | "CHAT" |
| `task_priority` | 0 |
| `task_preemptible` | false |
| `include_rag_hits` | true |
| `memory_rag_token_budget` | 1000 |

---

## 8. 注意事项

1. **chunks 数量不固定**：可以只有 message，可以只��� rag，也可以都有
2. **rag chunk 独立配置**：每个 rag chunk 有独立的 `uid`、`include_rag_hits`、`memory_rag_token_budget`
3. **memory_rag_token_budget = 0**：表示不使用记忆 RAG
4. **向后兼容**：可选字段不传时使用默认值