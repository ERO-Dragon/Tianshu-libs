# LLM 助手线设计说明

## 1. 目标

LLM 模块内部应拆成三条主线：

```text
LLM Module
├─ 底层推理调用线
├─ 助手对话线
└─ 其他模块调用线
```

本文只定义助手对话线。

助手对话线的目标不是简单拼接提示词，而是把玩家输入、记忆、静态 RAG、动态 RAG 和当前运行上下文编排成适合小模型推理的输入，让 LLM 表现为一个稳定、可持续演进的助手。

最低模型能力按 2B 级别设计，因此助手线必须控制上下文长度、避免把无关信息塞入 prompt，并依赖外部记忆与 RAG 补足模型知识。

## 2. 总体边界

### 2.1 助手线负责什么

助手线负责：

- 接收对话输入
- 读取五个记忆分区
- 检索静态 RAG
- 检索动态 RAG
- 组装当前轮上下文
- 调用底层推理调用线
- 接收流式输出
- 更新短期会话、会话摘要和长期记忆候选

### 2.2 助手线不负责什么

助手线不负责：

- 直接访问 Minecraft 活对象
- 直接执行游戏动作
- 直接调用其他业务模块实现类
- 直接绕过协议中心调用其他模块能力
- 把动态事实直接写成长期记忆
- 把模型输出无审核地写入长期记忆

### 2.3 和其他两条线的关系

```text
助手对话线
  ↓ 组装好的 messages / prompt
底层推理调用线
  ↓ stream / result
助手对话线
```

其他模块调用线面对外部模块调用。调用方应提供已经组织好的提示词或消息结构，LLM 不替调用方决定业务提示词。

## 3. 上下文系统分层

助手线的上下文来源分成三大系统：

```text
LLM Context System
├─ Memory System
│  ├─ Persona Memory
│  ├─ Short-term Conversation Memory
│  ├─ Conversation Summary Memory
│  ├─ Long-term User Memory
│  └─ Module Integration Memory
│
├─ RAG System
│  ├─ Static RAG
│  └─ Dynamic RAG
│
└─ Turn Context
   ├─ 当前用户输入
   ├─ 当前协议上下文
   └─ 当前即时事件
```

## 4. 五个记忆分区

五个记忆分区共同构成记忆体系。记忆体系记录的是长期有用、可复用或用于维持对话连续性的内容，不等同于 RAG。

### 4.1 Persona Memory

人设记忆用于描述助手是谁、说话风格、行为边界和世界观设定。

特点：

- 稳定
- 短小
- 高优先级
- 每轮通常都进入上下文

### 4.2 Short-term Conversation Memory

短期会话记忆保存最近若干轮对话原文。

特点：

- 保证最近对话连续性
- 有轮数或 token 上限
- 超出后进入摘要流程

### 4.3 Conversation Summary Memory

会话摘要记忆保存对长对话的语义压缩结果。

特点：

- 来自对话
- 是总结后的语义状态
- 用于低成本恢复上下文
- 不应与动态 RAG 混合

### 4.4 Long-term User Memory

长期用户记忆保存玩家偏好、长期目标、称呼习惯、稳定约定。

特点：

- 写入谨慎
- 需要明确稳定性
- 不保存高频变化事实
- 不保存临时坐标、临时背包、临时环境

### 4.5 Module Integration Memory

模块联动记忆保存模块能力、协作方式和稳定接口预设。

例如：

```text
CraftingGraph 模块可以提供配方路径解释。
Inventory 模块可以提供背包摘要。
TTS 模块可以朗读助手输出。
IR 模块负责把语音输入整理成可触发模块事件。
```

模块联动记忆记录的是稳定协作约定，不记录模块当前状态。

## 5. RAG 分区

RAG 体系和记忆体系并列存在。

### 5.1 Static RAG

静态 RAG 由 LLM Server 在启动阶段加载并索引，用于补足模型缺失的稳定知识。

静态 RAG 的输入通常由启动参数提供文件或目录路径，例如：

```text
--static-rag-path D:\\xxx\\knowledge
```

LLM Server 启动后负责：

- 读取静态知识文件或目录
- 将内容切分为可检索的文本块
- 调用 embedding 模型生成向量
- 建立常驻向量索引
- 在每次问答时用当前 query 检索 top-k 相关片段

静态知识块只在加载或重建索引时向量化，不在每次问答时重复向量化。每次问答只需要对当前 query 做一次 embedding，然后在已有静态索引中检索。

适合内容：

- Minecraft 固定机制
- 模组说明
- 模块文档
- 协议文档
- 常见玩法知识
- 固定规则和 FAQ
- 经过整理后适合长期复用的稳定知识

静态 RAG 更新频率可高可低，但它始终指的是“已经处理、已经索引、可以检索”的知识层，而不是原始数据源。

### 5.2 Dynamic RAG

动态 RAG 由主 JVM 在每次助手请求中组织好并发送给 LLM Server。它不要求来自物理文件，可以是请求体中的字符串列表、JSON 对象列表或结构化事实条目。

LLM Server 收到动态 RAG 后负责：

- 将动态条目转换为可 embedding 的检索文本
- 对当前请求内的动态条目进行实时向量化
- 对当前 query 进行向量化
- 在本次请求的动态向量集合中检索 top-k 相关条目
- 将命中的动态 RAG 结果交给上下文编排层

动态 RAG 默认不常驻索引。它面向当前请求和当前场景，通常每次请求重新向量化。动态 RAG 条目数量应保持小规模，适合几十条以内的短文本或结构化事实摘要。

### 5.3 Runtime Fact Source

运行时事实源由主 JVM 维护，只负责采集、快照化、清洗、结构化和组织动态 RAG 候选，不直接承担模型推理。

运行时事实源不保存：

- 总结
- 推断
- 观点
- 建议
- 模型生成内容
- 长期偏好
- 对事实的语义压缩

运行时事实源的定位是“为动态 RAG 提供干净、结构化、当前有效的事实输入”，不是 LLM Server 内部的长期记忆层，也不是静态 RAG 索引层。

## 6. 动态 RAG 收录原则

动态 RAG 不是日志系统，也不是所有运行时数据的垃圾桶。它的职责不是保存世界发生了什么，而是保存“当前回合还能直接用来帮助推理的客观事实”。因此，动态 RAG 在发送给 LLM Server 之前，所有候选内容都必须先经过主 JVM 的快照化、清洗和结构化处理。

LLM Server 不负责采集 Minecraft 运行态，也不负责判断原始事实是否可信。LLM Server 只负责对本次请求携带的动态 RAG 候选进行向量化、检索、排序和上下文融合。

### 6.1 动态事实的基本形态

动态 RAG 里的最小存储单元不是自然语言段落，而是结构化事实条目。每条条目应尽量保持稳定、可覆盖、可过期、可追踪。

推荐结构：

```text
DynamicFact {
  factId
  type
  scope
  source
  subject
  fields
  tags
  importance
  updatedAt
  ttl
  version
}
```

其中：

- `type` 用来描述事实类别
- `scope` 用来描述作用范围
- `source` 用来描述事实来源
- `subject` 用来描述事实指向的对象
- `fields` 用来承载实际数据
- `tags` 用来支持轻量路由与排序
- `importance` 用来控制上下文优先级
- `updatedAt` 和 `ttl` 用来控制新鲜度与过期
- `version` 用来支持覆盖和增量更新

### 6.2 必须先清洗

所有候选事实进入动态 RAG 前都应经过清洗流程：

```text
原始运行态数据
  ↓
平台侧快照化
  ↓
事实归一化
  ↓
字段裁剪
  ↓
去噪
  ↓
去重 / 覆盖旧值
  ↓
设置来源、时间戳、TTL、重要级别
  ↓
写入 Dynamic RAG
```

清洗目标：

- 去掉 Minecraft 活对象
- 去掉无关字段
- 去掉高频噪声
- 保留客观事实
- 保留可检索字段
- 保留过期策略

### 6.3 收录判断标准

一个事实进入动态 RAG 前应满足以下条件：

1. 是客观事实
2. 对当前或近期对话推理可能有用
3. 可被稳定更新或覆盖
4. 有明确来源
5. 有时间戳或版本
6. 有过期策略
7. 检索收益大于记录成本

### 6.4 适合进入动态 RAG 的内容

适合内容包括：

- 当前维度
- 当前坐标
- 当前朝向
- 当前天气
- 当前时间
- 当前准星目标
- 当前背包摘要
- 当前装备摘要
- 当前生命、饥饿、状态效果
- 当前模块 ready / failed / busy 状态
- 当前正在处理的任务 id
- 最近一次关键事件事实

### 6.5 不适合进入动态 RAG 的内容

不适合内容包括：

- 每 tick 坐标历史
- 每帧视角变化
- 完整原始背包长文本
- 全量世界方块数据
- 大段原始聊天历史
- 模型输出总结
- 玩家偏好
- 模块能力的长期说明
- 未清洗的日志

### 6.6 世界坐标如何处理

世界坐标可以进入动态 RAG，但不应按日志方式持续堆积。

推荐方式：

```text
fact.player.location.current = {
  dimension,
  x,
  y,
  z,
  yaw,
  pitch,
  updatedAt,
  ttl
}
```

只保留当前值，必要时保留最近关键变化点。不记录无意义的连续轨迹。

### 6.7 动态事实生命周期

动态事实的生命周期应以覆盖和过期为主，而不是持续追加。

推荐生命周期：

```text
事实源产生候选事实
  ↓
主 JVM 快照化
  ↓
主 JVM 清洗与结构化
  ↓
写入 RuntimeFactPool
  ↓
按 factId / type / scope 覆盖旧值
  ↓
按 ttl / version 淘汰过期值
  ↓
按当前助手请求生成候选事实包
  ↓
发送给 LLM Server
  ↓
LLM Server 参与上下文编排
```

动态事实默认不追求长期留存。对于高频变化事实，优先保留当前值；对于低频关键事件，可以保留最近少量事件窗口；对于已经过期或被覆盖的事实，应直接移除。

### 6.8 动态事实发送策略

主 JVM 发送给 LLM Server 的内容不应是全量世界状态，而应是经过裁剪后的动态 RAG 候选包。

动态 RAG 候选包可以包含：

- 当前玩家基础状态
- 当前场景状态
- 当前模块状态
- 最近关键事件
- 与当前助手请求相关的轻量候选事实

动态事实包不应包含：

- Minecraft 活对象
- 未清洗日志
- 全量背包原始长文本
- 全量世界数据
- 高频连续轨迹
- 已经过期的事实

主 JVM 只保证事实真实、干净、结构化；LLM Server 负责在这些候选中进行向量检索，并决定哪些动态 RAG 命中结果进入最终上下文。

推荐请求形态保持简单，`dynamic_rag` 只传一个条目列表，不额外传 RAG 参数：

```json
{
  "messages": [],
  "dynamic_rag": [
    "玩家当前在 Overworld，坐标 X=120 Y=64 Z=-32。",
    "玩家背包里有 3 个铁锭、2 根木棍和一把铁剑。",
    "附近有僵尸，当前时间是夜晚。"
  ]
}
```

如果模组侧更适合传对象，也只需要保证每个对象能被服务器转换成检索文本：

```json
{
  "messages": [],
  "dynamic_rag": [
    {
      "text": "玩家当前在 Overworld，坐标 X=120 Y=64 Z=-32。"
    },
    {
      "text": "附近有僵尸，当前时间是夜晚。"
    }
  ]
}
```

动态 RAG 的 top-k、排序权重和上下文预算由 LLM Server 使用默认策略处理，模组侧不需要每次传参数。

## 7. 动态 RAG 与记忆的区别

### 7.1 动态 RAG 回答“现在是什么”

例如：

```text
当前玩家在 Overworld，坐标 X=120 Y=64 Z=-32。
当前背包有 3 个铁锭和 2 根木棍。
CraftingGraph 当前处于 ready。
```

### 7.2 记忆回答“长期如何对待”

例如：

```text
玩家喜欢简洁回答。
玩家偏好工业风建筑。
玩家希望助手优先考虑生存模式可行性。
```

### 7.3 关键规则

动态 RAG 事实默认不进入长期记忆。只有当某个事实被确认具有长期稳定意义，且经过记忆写入策略判断后，才可以生成长期记忆候选。

## 8. 助手线执行流程

```text
用户输入
  ↓
AssistantInputNormalizer
  ↓
AssistantContextCollector
  ├─ MemorySystem
  ├─ StaticRag
  ├─ RuntimeFactSource -> DynamicRag
  └─ TurnContext
  ↓
AssistantContextOrchestrator
  ↓
底层推理调用线
  ↓
AssistantOutputProcessor
  ↓
返回流式输出或完整结果 + MemoryUpdatePlanner
```

### 8.1 AssistantInputNormalizer

输入归一层主要位于主 JVM，用于把语音对话、聊天框输入、UI 输入或其他模块转发的对话请求统一组织为服务器可消费的助手请求。

发送给 LLM Server 的请求至少应包含：

输入来源可以包括：

- 语音对话
- 聊天框输入
- UI 输入
- 其他模块转发的对话请求

### 8.2 AssistantContextCollector

上下文采集层负责从记忆、RAG、运行时事实源和当前运行态中收集候选上下文。

它只采集候选内容，不直接决定最终 prompt。

### 8.3 AssistantContextOrchestrator

上下文编排层负责把不同来源的候选内容整理成最终 messages。它不是简单拼接，而是按 token 预算、上下文焦点和事实价值进行分层编排。

建议处理顺序：

1. 人设记忆
2. 长期用户记忆
3. 模块联动记忆
4. 当前动态事实
5. 静态 RAG 命中知识
6. 会话摘要记忆
7. 最近短期会话
8. 当前用户输入

动态事实必须来自已经清洗好的事实条目，不得直接把活对象或原始运行态对象塞进上下文。

### 8.4 AssistantOutputProcessor

输出处理层负责处理模型流式输出和最终结果。

职责包括：

- 合并或转发流式文本
- 更新短期会话记忆
- 判断是否需要摘要
- 生成长期记忆候选
- 不直接把模型输出写入长期记忆

### 8.5 MemoryUpdatePlanner

记忆更新规划器负责决定哪些内容进入哪个记忆分区。

它不应写入动态 RAG。动态 RAG 由事实源和事实清洗管线维护。

## 9. 上下文预算策略

2B 模型下必须严格控制上下文预算。

建议策略：

- 人设短小固定
- 长期记忆只召回相关条目
- 动态 RAG 只召回当前问题相关事实
- 静态 RAG top-k 较小
- 最近对话保留有限轮数
- 长对话依赖摘要
- 不把全量模块状态塞入 prompt

## 10. 模块联动接口预留

其他模块不应直接修改助手内部状态，而应通过稳定接口提供信息。

建议未来预留：

```text
AssistantMemoryProvider
DynamicFactProvider
StaticKnowledgeProvider
AssistantContextContributor
```

### 10.1 DynamicFactProvider

模块通过 DynamicFactProvider 提供客观事实候选。

候选事实必须经过清洗后才能进入动态 RAG。

### 10.2 StaticKnowledgeProvider

模块通过 StaticKnowledgeProvider 提供可离线索引的稳定知识源，或者提供静态知识文件 / 目录路径。

这类内容进入静态 RAG，而不是记忆层。

### 10.3 AssistantContextContributor

模块通过 AssistantContextContributor 提供会话期可用的补充上下文候选，由助手线决定是否纳入最终 messages。

## 11. 与协议中心的关系

助手线内部可以作为 LLM 模块的一部分存在，但跨模块输入输出仍应通过协议中心。

协议中心负责：

- 路由
- 排队
- 限流
- 生命周期状态
- 取消
- 死信

助手线负责：

- 对话上下文编排
- 记忆召回
- RAG 召回
- 输出后处理

两者不能互相替代。

## 12. 主 JVM 与 LLM Server 的职责边界

主 JVM 负责 Minecraft 运行态、事实采集、记忆维护和动态 RAG 候选组织；LLM Server 负责模型推理、embedding、动态 RAG 检索、静态 RAG 索引与上下文编排。

### 12.1 主 JVM 负责什么

主 JVM 更适合做这些事：

- 采集当前世界和模块状态
- 把活对象快照化
- 清洗动态事实
- 维护短生命周期事实仓
- 维护助手侧记忆和摘要
- 生成结构化动态 RAG 条目
- 按当前助手请求组织动态 RAG 候选包
- 调用 LLM Server 并接收流式输出

主 JVM 不应长期保存重型向量索引，也不应为了 RAG 检索额外跑一套高成本向量模型。

### 12.2 LLM Server 负责什么

LLM Server 更适合做这些事：

- 执行 chat 推理
- 维护 chat 模型和 embedding 模型服务进程
- 启动时加载静态 RAG 文件或目录
- 对静态 RAG 切块、embedding 并建立常驻索引
- 接收主 JVM 每次请求发送的动态 RAG 候选条目
- 对动态 RAG 候选做请求内实时向量化和 top-k 检索
- 对当前 query 做 embedding，并同时检索动态 RAG 与静态 RAG
- 将动态 RAG 命中、静态 RAG 命中、messages 和推理参数统一编排
- 处理流式输出、输出清洗、token 上限和取消

### 12.3 分工原则

原则是：

- 主 JVM 保持轻量，负责事实采集、记忆维护和动态 RAG 候选组织
- LLM Server 保持推理聚焦，负责 embedding、RAG 检索
- 动态 RAG 由主 JVM 组织候选，由 LLM Server 在请求内实时检索
- 静态 RAG 由 LLM Server 启动时加载并常驻索引
- 不把所有检索压力都堆到主 JVM

## 13. 动态 RAG 向量检索策略

动态 RAG 默认使用请求内临时向量检索。主 JVM 发送本轮候选条目，LLM Server 对这些条目实时 embedding，并用当前 query 检索最相关结果。

动态 RAG 不要求来自物理文件。只要请求体能提供可转换为文本的条目，就可以进入向量检索流程。

### 13.1 为什么动态 RAG 可以实时向量化

动态 RAG 通常是当前回合相关的小规模候选包，具备这些特点：

- 条目数量少
- 内容短
- 时效性强
- 每轮变化可能较大
- 不适合长期常驻索引

因此动态 RAG 适合每次请求实时向量化。只要条目规模控制在几十条以内，额外耗时通常可接受。

### 13.2 动态 RAG 检索流程

```text
当前用户输入 / 当前 query
  ↓
query embedding
  ↓
请求内 dynamic_rag 条目文本化
  ↓
dynamic_rag embedding
  ↓
向量相似度计算
  ↓
按相似度和服务器默认策略排序
  ↓
返回少量动态 RAG 命中结果
```

### 13.3 动态 RAG 条目建议字段

动态 RAG 条目建议尽量保持简单，优先用字符串列表；如果需要对象形式，也只保留一个可转文本字段 `text`。

### 13.4 静态 RAG 与动态 RAG 的向量化区别

| 类型 | 来源 | 向量化时机 | 是否每轮重算 |
|---|---|---|---|
| 静态 RAG | 启动参数指定的文件或目录 | LLM Server 启动或重建索引时 | 否 |
| 动态 RAG | 每次请求体携带 | 每次请求处理时 | 是 |
| 当前 query | 当前用户输入或检索查询 | 每次请求处理时 | 是 |

### 13.6 什么时候还能进一步优化

如果后续动态 RAG 条目量变大，或者同一批候选在短时间内被频繁复用，可以再考虑：

- 复用本轮动态条目的缓存向量
- 对重复条目做去重
- 对超长条目先做裁剪再 embedding

但第一版不必引入复杂缓存，先保证请求内实时检索链路稳定即可。

## 14. 阶段性落地顺序

推荐顺序：

1. 先定义助手请求协议，明确 `messages`、`dynamic_rag` 和推理参数
2. 建立静态 RAG 启动加载与常驻索引能力
3. 建立动态 RAG 请求内实时向量化与检索能力
4. 建立 AssistantContextOrchestrator
5. 再收敛底层推理调用线为纯执行器
6. 最后再对接协议入口和其他模块调用线

## 15. 结论

助手线不是补丁式的聊天函数，而是 LLM 模块内部的对话运行时。

它的健康边界是：

- 记忆保存长期语义
- 静态 RAG 保存稳定知识
- 动态 RAG 保存清洗后的客观事实
- 当前上下文保存本轮输入
- 底层调用线只负责推理执行

运行时事实必须先清洗再写入，且只记录有价值的客观事实。这样才能避免运行时事实污染记忆，也能避免 2B 模型被无关上下文淹没。
